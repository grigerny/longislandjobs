<%= render '/home/index' %>

<div class="container">
	
	<div class="row-fluid">
		<div class="span9">
			<div id="location-index">
				<legend>Choose a location
				</legend>
				
			</div>
		</div>	
	</div>		

<% @locations.each do |location| %>
<%= link_to "#{location.city}", location_jobs_path(location) %><br>
<% end %>

</div>
