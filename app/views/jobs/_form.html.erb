<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<div class="row-fluid">
		<div class="span12">
			Job Title: <%= f.text_field :title, :class => "job-title-field" %><br>
		</div>
	</div>

	<div class="row-fluid">
		<div class="span12">
			Description: <%= f.text_area :description, :class => "job-description-field" %>
		</div>
	</div>
	
	<div class="row-fluid">
		<div class="span9">
			<div id="new-job-submit">
	 			<%= f.submit "Add Job" %>
			<div>
		</div>
	</div>
<% end %>
